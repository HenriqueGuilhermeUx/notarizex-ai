# Kapso webhook secrets

Production runtime requires `KAPSO_PROJECT_WEBHOOK_SECRET` and `KAPSO_MESSAGE_WEBHOOK_SECRET` as secret Netlify environment variables for Functions/Runtime.

Configured on 2026-09-23. Secret values are intentionally not stored in the repository.
