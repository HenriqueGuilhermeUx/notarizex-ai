const fetch=require('node-fetch');
const headers={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type','Access-Control-Allow-Methods':'OPTIONS,POST'};
function reply(c,b){return{statusCode:c,headers,body:JSON.stringify(b)}}
function base(){return process.env.WOOVI_ENV==='sandbox'?'https://api.woovi-sandbox.com':'https://api.openpix.com.br'}
function admin(t){const a=process.env.SMARTBOTS_ADMIN_TOKEN||process.env.ADMIN_TOKEN;return a&&t&&t===a}
exports.handler=async(event)=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers,body:''};try{const b=JSON.parse(event.body||'{}');if(!admin(String(b.adminToken||'')))return reply(403,{success:false,error:'Admin token invalido'});const token=process.env.WOOVI_TOKEN||process.env.OPENPIX_TOKEN;if(!token)throw new Error('WOOVI_TOKEN nao configurado');const site=process.env.URL||process.env.DEPLOY_PRIME_URL||'https://smartbots.club';const url=b.url||site+'/.netlify/functions/woovi-webhook';const payload={webhook:{name:'SmartBots Charge Completed',event:'OPENPIX:CHARGE_COMPLETED',url,authorization:process.env.WOOVI_WEBHOOK_SECRET||'smartbots',isActive:true}};const r=await fetch(base()+'/api/v1/webhook',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify(payload)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(JSON.stringify(j));return reply(200,{success:true,webhook:j,url})}catch(e){return reply(500,{success:false,error:e.message})}};
