const fetch=require('node-fetch');
const headers={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type','Access-Control-Allow-Methods':'OPTIONS,POST'};
function reply(c,b){return{statusCode:c,headers,body:JSON.stringify(b)}}
function clean(v){return String(v||'').trim()}
function amount(p){if(p==='Essencial')return 4900;if(p==='Premium')return 14900;return 7900}
function apiBase(){return process.env.WOOVI_ENV==='sandbox'?'https://api.woovi-sandbox.com':'https://api.openpix.com.br'}
async function sb(path,opt={}){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_ANON_KEY;if(!url||!key)throw new Error('Supabase nao configurado.');return fetch(url+'/rest/v1/'+path,{...opt,headers:{'Content-Type':'application/json',apikey:key,Authorization:'Bearer '+key,...(opt.headers||{})}})}
async function auth(botId,token){const r=await sb('website_bots?bot_id=eq.'+encodeURIComponent(botId)+'&client_token=eq.'+encodeURIComponent(token)+'&select=bot_id,company_name,email,owner_email,plan');const a=r.ok?await r.json():[];return a[0]||null}
exports.handler=async(event)=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers,body:''};if(event.httpMethod!=='POST')return reply(405,{success:false,error:'Method Not Allowed'});try{const body=JSON.parse(event.body||'{}');const botId=clean(body.botId),token=clean(body.clientToken);if(!botId||!token)return reply(400,{success:false,error:'botId e clientToken obrigatorios'});const bot=await auth(botId,token);if(!bot)return reply(403,{success:false,error:'Acesso negado'});const wooviToken=process.env.WOOVI_TOKEN||process.env.OPENPIX_TOKEN;if(!wooviToken)throw new Error('WOOVI_TOKEN nao configurado');const plan=clean(body.plan)||bot.plan||'Profissional';const value=amount(plan);const correlationID='sb_'+botId+'_'+Date.now();const payload={correlationID,value,comment:'SmartBots '+plan,customer:{name:bot.company_name||'SmartBots',email:bot.email||bot.owner_email||''}};const wr=await fetch(apiBase()+'/api/v1/charge',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+wooviToken},body:JSON.stringify(payload)});const wj=await wr.json().catch(()=>({}));if(!wr.ok)throw new Error(JSON.stringify(wj));const ch=wj.charge||wj;const patch={bot_id:botId,company_name:bot.company_name,customer_email:bot.email||bot.owner_email,plan,status:'pending',amount_cents:value,billing_cycle:'monthly',woovi_correlation_id:correlationID,woovi_charge_id:ch.globalID||ch.chargeId||ch.id||'',woovi_payment_link:ch.paymentLinkUrl||'',woovi_qr_code_image:ch.qrCodeImage||'',woovi_br_code:ch.brCode||(ch.paymentMethods&&ch.paymentMethods.pix&&ch.paymentMethods.pix.brCode)||'',woovi_status:ch.status||'ACTIVE',woovi_payload:wj,updated_at:new Date().toISOString()};const ex=await sb('smartbot_subscriptions?bot_id=eq.'+encodeURIComponent(botId)+'&limit=1');const rows=ex.ok?await ex.json():[];let sr;if(rows.length)sr=await sb('smartbot_subscriptions?id=eq.'+rows[0].id,{method:'PATCH',headers:{Prefer:'return=representation'},body:JSON.stringify(patch)});else sr=await sb('smartbot_subscriptions',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify(patch)});if(!sr.ok)throw new Error(await sr.text());return reply(200,{success:true,charge:ch,subscription:(await sr.json())[0]})}catch(e){return reply(500,{success:false,error:e.message})}};
