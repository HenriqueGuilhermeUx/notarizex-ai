const crypto=require('crypto');
const fetch=require('node-fetch');
const {validateAdminSession}=require('./smartbots-admin-auth');

const headers={'Content-Type':'application/json','Cache-Control':'no-store','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, X-Admin-Key, X-Admin-Session','Access-Control-Allow-Methods':'OPTIONS, POST'};
function reply(c,b){return{statusCode:c,headers,body:JSON.stringify(b)}}
function clean(v,max=0){const s=String(v||'').trim();return max?s.slice(0,max):s}
function safeEqual(a,b){if(!a||!b)return false;const x=Buffer.from(String(a)),y=Buffer.from(String(b));return x.length===y.length&&crypto.timingSafeEqual(x,y)}
async function authorized(event,body){const legacy=clean((event.headers&&(event.headers['x-admin-key']||event.headers['X-Admin-Key']))||body.adminToken),expected=process.env.SMARTBOTS_ADMIN_API_KEY||process.env.SMARTBOTS_ADMIN_TOKEN||process.env.ADMIN_TOKEN;if(expected&&legacy&&safeEqual(legacy,expected))return true;const session=clean((event.headers&&(event.headers['x-admin-session']||event.headers['X-Admin-Session']))||body.sessionToken,300);return validateAdminSession(session)}
async function db(path,opt={}){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error('Supabase service role não configurado.');return fetch(url+'/rest/v1/'+path,{...opt,headers:{'Content-Type':'application/json',apikey:key,Authorization:'Bearer '+key,...(opt.headers||{})}})}
async function rows(res,label){if(!res.ok)throw new Error(label+': '+(await res.text()).slice(0,700));const raw=await res.text();return raw?JSON.parse(raw):[]}
async function listHelp(){return rows(await db('smartbot_whatsapp_onboarding_help?status=in.(pending,in_progress)&select=id,bot_id,company_name,contact_name,contact_email,contact_whatsapp,selected_mode,status,requested_at,last_requested_at,notified_at,metadata&order=last_requested_at.desc&limit=200'),'listar ajuda WhatsApp')}
async function updateHelp(id,status){const allowed=status==='in_progress'?'in_progress':'resolved',now=new Date().toISOString(),patch={status:allowed,updated_at:now};if(allowed==='resolved')patch.resolved_at=now;const data=await rows(await db('smartbot_whatsapp_onboarding_help?id=eq.'+encodeURIComponent(id),{method:'PATCH',headers:{Prefer:'return=representation'},body:JSON.stringify(patch)}),'atualizar ajuda WhatsApp');if(!data[0])throw new Error('Pedido de ajuda não encontrado.');return data[0]}
exports.handler=async event=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers,body:''};if(event.httpMethod!=='POST')return reply(405,{success:false,error:'Method Not Allowed'});let body={};try{body=JSON.parse(event.body||'{}')}catch{return reply(400,{success:false,error:'JSON inválido.'})}if(!(await authorized(event,body)))return reply(401,{success:false,error:'Sessão administrativa inválida ou expirada.'});try{const action=clean(body.action||'list');if(action==='list'){const items=await listHelp();return reply(200,{success:true,total:items.length,items})}if(action==='start'||action==='resolve'){const id=Number(body.id);if(!Number.isFinite(id)||id<=0)return reply(422,{success:false,error:'Pedido inválido.'});const item=await updateHelp(id,action==='start'?'in_progress':'resolved');return reply(200,{success:true,item})}return reply(422,{success:false,error:'Ação inválida.'})}catch(error){console.error('[AdminWhatsappHelp]',error.message);return reply(500,{success:false,error:error.message||'Falha interna.'})}};