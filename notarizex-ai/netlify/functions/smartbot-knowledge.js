const fetch=require('node-fetch');
const headers={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type','Access-Control-Allow-Methods':'OPTIONS,POST'};
function reply(c,b){return{statusCode:c,headers,body:JSON.stringify(b)}}
function clean(v){return String(v||'').trim()}
async function db(path,opt={}){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_ANON_KEY;if(!url||!key)throw new Error('Supabase nao configurado');return fetch(url+'/rest/v1/'+path,{...opt,headers:{'Content-Type':'application/json',apikey:key,Authorization:'Bearer '+key,...(opt.headers||{})}})}
async function auth(botId,token){const r=await db('website_bots?bot_id=eq.'+encodeURIComponent(botId)+'&client_token=eq.'+encodeURIComponent(token)+'&select=bot_id&limit=1');const a=r.ok?await r.json():[];return !!a[0]}
exports.handler=async(ev)=>{if(ev.httpMethod==='OPTIONS')return{statusCode:204,headers,body:''};if(ev.httpMethod!=='POST')return reply(405,{success:false,error:'Method Not Allowed'});try{const b=JSON.parse(ev.body||'{}');const botId=clean(b.botId),token=clean(b.clientToken);if(!botId||!token)return reply(400,{success:false,error:'botId e clientToken obrigatorios'});if(!await auth(botId,token))return reply(403,{success:false,error:'Acesso negado'});if(b.action==='save'){const content=clean(b.content);if(!content)return reply(400,{success:false,error:'Conteudo obrigatorio'});const r=await db('smartbot_knowledge',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({bot_id:botId,title:clean(b.title)||'Base principal',content,is_active:true,updated_at:new Date().toISOString()})});if(!r.ok)throw new Error(await r.text());return reply(200,{success:true,item:(await r.json())[0]})}const r=await db('smartbot_knowledge?bot_id=eq.'+encodeURIComponent(botId)+'&order=updated_at.desc&limit=20');return reply(200,{success:true,items:r.ok?await r.json():[]})}catch(e){return reply(500,{success:false,error:e.message})}};
