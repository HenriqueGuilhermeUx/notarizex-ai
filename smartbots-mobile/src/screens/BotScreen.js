import React from 'react';
import * as Clipboard from 'expo-clipboard';
import * as Linking from 'expo-linking';
import {Screen,H1,Muted,Card,Button,Row} from '../components';
import {apiBase} from '../theme';
export default function BotScreen({session}){const link=session.miniSiteUrl||apiBase+'/b/'+session.botId;const panel=apiBase+'/comece-aqui.html';async function copy(v){await Clipboard.setStringAsync(v)}return <Screen><H1>Meu Bot</H1><Muted>Mini site, bot e link da bio para divulgar seu negócio.</Muted><Card green><Row title={session.businessName||'Meu negócio'} subtitle="Seu bot SmartBots"/><Muted>Compartilhe o link abaixo no Instagram, WhatsApp, QR Code, cartão e anúncios.</Muted></Card><Card><Row title="Mini site SmartBots" subtitle={link}/><Button onPress={()=>copy(link)}>Copiar link</Button><Button variant="dark" onPress={()=>Linking.openURL(link)}>Testar bot</Button></Card><Row title="Colocar na bio do Instagram" subtitle="Use esse link como recepção digital."/><Row title="Criar QR Code" subtitle="Abra o link no painel web e gere material de divulgação." onPress={()=>Linking.openURL(apiBase+'/instagram-onboarding.html')}/><Row title="Configuração completa" subtitle="Treinamento do bot e ajustes avançados ficam no painel web." onPress={()=>Linking.openURL(panel)}/></Screen>}
